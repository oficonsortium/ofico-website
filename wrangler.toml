name = "ofico-website"
compatibility_date = "2026-09-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"

# Development / preview: `npm run deploy:dev`
[env.dev]
name = "ofico-website-dev"

# Production: `npm run deploy` (custom domain attached in the Cloudflare dashboard)
[env.production]
name = "ofico-website"
